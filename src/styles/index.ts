import styled from 'styled-components';

export const ItemContainer = styled.div`
  margin: 0 2px;
  position: relative;

  p {
    position: absolute;
    bottom: 0;
    right: 0;
    line-height: 16px;
  }

  img {
    width: 100%;
    height: 100%;
  }
`;
